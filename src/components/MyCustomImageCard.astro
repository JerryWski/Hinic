---
import { Image } from "astro:assets";

interface Props {
    imagePath: string,
    altText: string,
}

const {imagePath, altText} = Astro.props as Props;
const images = import.meta.glob('/src/assets/*.{webp,png}')
---

 <Image class='benefit-image' src={images[imagePath]()} alt={altText} />
